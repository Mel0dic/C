#include <stdio.h>

#define MAX 15
#define ALPHA 26
#define IN 0
#define OUT 1

int main(){

        int c, i, state;

        int alphabet[ALPHA], word[MAX];

        for(i = 0; i < ALPHA; i++){

                digits[i] = 0;

        }

        while((c = getchar()) != EOF){

                if(c == ' '|| c == '\n' || c == '\t'){

                        state == IN;

                }else if(){



                }else{

                          ++ndigit[c-'0'];

                }

        }

        for(i = 0; i < 10; i++){

                printf("%d", digits[i]);

                for(i = 0; i < len[]; i++){



                }

                printf("\n");

        }

}
