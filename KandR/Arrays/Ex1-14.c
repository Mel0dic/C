#include <stdio.h>

#define MAX 15
#define IN 0
#define OUT 1

int main(){


        int digits[MAX];

        for(i = 0; i < MAX; i++){

                digits[i] = 0;

        }

        while((c = getchar()) != EOF){

                if(c == ' '|| c == '\n' || c == '\t'){



                }

        }

        for(i = 0; i < 10; i++){

                printf("%d", digits[i]);

                for(i = 0; i < len[]; i++){



                }

                printf("\n");

        }

}
